#!/usr/bin/env python
'''Add TripHistory data to the message before it gets passed to Broca. '''

def add(data):
    '''data is a validated JSON-parsed dictionary. Data is mutable, so we add the field
    to 'data' itself. On failure, do nothing. '''
    pass